algoritmo exercicio6
	inicio
		real a, b, x
		escreva("Digite a quantidade de cerca utilizada na Rua do Sol (em metros)")
		leia(a)
		escreva("Digite a quantidade de cerca utilizada na Rua dos Pássaros Verdes (em metros)")
		leia(b)
		x = raiz(a*a + b*b)
		escreva("Será necessário comprar " + x + "metros de cerca")
	fim 