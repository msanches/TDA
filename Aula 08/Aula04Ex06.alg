algoritmo aula04Ex06
    real raio
    início
        escreva("Digite o raio do círculo em cm:")
        leia(raio)
        escreva("A área do círculo é " + calculaArea(raio) + "cm²")
    fim

real calculaArea(real raio)
    real area
    area = PI *r*r
    retorne area
    fim