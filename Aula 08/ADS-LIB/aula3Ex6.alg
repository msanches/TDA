algoritmo aula3Ex6
	inicio
		real produto
		escreva("Digite o valor do produto")
		leia(produto)

		escreva("O valor do produto será de R$ " + calculaDesconto(valor))
	fim 

real calculaDesconto(real valor)
	real desconto
	desconto = valor * 0.91
	retorne(velocidade)
	fim
//exemplo acrescenta 5% valor = valor * 1.05
//exemplo2 desconta 5% valor = valor * 0.95